название: marlin_auto_build

на:
 # срабатывает, если мы изменяем сборку
  нажатием:
 пути:
 - 'сборки /**'
  # запускается каждую ночь в 03:30 по
  расписанию:
 - cron: '30 3 * * * "

задания:
 create_builds:
 запуск: ubuntu-последние 
    шаги:
 - использует: zisismaras/marlin_auto_build@v1
